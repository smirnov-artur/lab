{
  "name": "lab",
  "version": "1.0.0",
  "description": "Smirnov Lab — WebGPU / TSL experiments",
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --format=esm --target=es2022 --legal-comments=none --outfile=app.js && esbuild src/fallback.js --bundle --minify --format=esm --target=es2022 --legal-comments=none --outfile=fallback.js",
    "serve": "node scripts/serve.js"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright": "^1.62.1"
  },
  "private": true,
  "type": "module"
}
